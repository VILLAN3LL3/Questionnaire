@using Questionnaire.Data
@inject QuestionService QuestionService

<h4 class="mt-4">@Question.QuestionText</h4>


@if (_vm == null)
{
    <p><em>Loading...</em></p>
}
else
{
    @if (_vm.SelectedAnswer != null)
    {
        @if (_vm.RightAnswerSelected)
        {
            <p class="pl-3">Your answer '@(_vm.SelectedAnswer.OptionText)' is correct.</p>
        }
        else
        {
            <p class="pl-3">
                Your answer '@(_vm.SelectedAnswer.OptionText)' is wrong.<br />
                Correct answer: '@(_vm.CorrectAnswer.OptionText)'
            </p>
        }
    }
    else
    {
        <p class="pl-3">You haven't answered this question yet.</p>
    }
}




@code {
    [Parameter]
    public Question Question { get; set; }
    private QuestionEvaluationVM _vm;

    protected override void OnInitialized()
    {
        _vm = QuestionService.EvaluateQuestion(Question);
    }
}
